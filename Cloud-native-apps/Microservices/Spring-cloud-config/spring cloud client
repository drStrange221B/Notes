add dependency

spring-cloud-starter-config
dependencyManagement

application.properties

spring.application.name= application name must match the name of configuration file name
spring.profiles.active= 
spring.config.import=configserver:http://<config-server-domain-name>


@Configuration
@ConfigurationProperties(prefix="accounts")
@Getter
@Setters
@ToString
public class <ClassName>{

}

## Refresh the cloud config properties
@RefreshScope

eg
@RefreshScope
@SpringBootApplication

## expose all the Endpoint for the spring Actuator in application.properties file
management.endpoints.web.exposure.include=*

## Check all the endpoint of springboot actuator
<DomainName>:port/actuator
eg localhost:8080/actuator


